"use strict";
let pseudo = {
	id: "name",
	gh: "last-name",
	length: 2,
};
let array = Array.from(pseudo);

console.log(array);

for (const element of array) {
	console.log(element);
}
/* array.pop();
console.log(array); */
